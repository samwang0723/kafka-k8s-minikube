apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka.name" . }}-test-connection"
  labels:
{{ include "kafka.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kafka.name" . }}:{{ .Values.ports.internal.port }}']
  restartPolicy: Never
